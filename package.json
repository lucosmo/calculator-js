{
  "type": "module",
  "devDependencies": {
    "@babel/plugin-transform-modules-commonjs": "^7.17.7",
    "jest": "^27.5.1"
  },
  "scripts": {
    "test": "jest --env=jsdom",
    "coverage": "jest --env=jsdom --coverage"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  }
}
